fastapi==0.115.0
uvicorn[standard]>=0.35.0
sqlalchemy==2.0.36
alembic==1.13.3
asyncpg==0.30.0
psycopg2-binary==2.9.10
python-dotenv>=1.1.0
pydantic-settings==2.6.1
argon2-cffi==23.1.0
cryptography>=42.0.0
python-jose[cryptography]==3.3.0
pyotp==2.9.0
qrcode[pil]==7.4.2
pillow==10.4.0
azure-cognitiveservices-vision-face==0.6.0
msrest==0.7.1
requests==2.32.3
APScheduler==3.10.4
twilio==9.3.4
reportlab==4.2.5
openpyxl==3.1.5
email-validator==2.2.0
python-multipart==0.0.12
httpx>=0.28.1,<1.0.0
exponent_server_sdk==2.1.0
slowapi==0.1.9
PyMuPDF==1.24.10
aiofiles==24.1.0
agora-token-builder==1.0.0
beautifulsoup4==4.12.3
lxml==5.3.0
bleach>=6.0.0
# AI providers — conditionally imported by live_session_ai / classpulse_ai /
# career / suggestions. Pin to avoid breakage on fresh installs.
google-genai>=0.3.0
groq>=0.11.0
# DeepSeek is accessed via plain HTTP (requests) — no SDK needed.

# Sentry — error + performance monitoring. Initialised in main.py only
# when SENTRY_DSN is set (app still starts perfectly without it).
sentry-sdk[fastapi]>=2.18.0

# AWS SDK — used by utils/s3_utils.py for leave-document uploads.
# All calls are guarded so the backend still runs without AWS creds.
boto3==1.35.0

# TODO: Enable Prometheus metrics when needed
# To re-enable: uncomment the line below, run `pip install -r requirements.txt`,
# and set up a Prometheus scrape target pointing at /metrics.
# prometheus-fastapi-instrumentator==0.9.7

# ── Testing (issue #77) ─────────────────────────────────────────────
# Comprehensive pytest suite under backend/tests. Run with `make test`
# or `pytest tests/ -v`. httpx is already pinned above (>=0.28.1) and is
# reused by Starlette's TestClient — NOT re-pinned to 0.27.0 to avoid a
# version conflict.
pytest==8.3.2
pytest-asyncio==0.23.8
pytest-timeout==2.3.1
factory-boy==3.3.1
