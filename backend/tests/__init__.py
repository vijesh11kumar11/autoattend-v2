"""
AutoAttend AI v2.0 — Test suite package (issue #77).

The whole suite runs against a DEDICATED PostgreSQL database that is
completely separate from the production / development database. The
production database is never touched.
"""
